**Unreleased**
* Added 'git checkout' action
* Added set upstream option to 'git push' action
* Updated action result behavior: `branch_name` now returns the repository's current checked-out working branch (HEAD) at runtime
